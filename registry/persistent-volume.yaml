apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-data
  namespace: ns-registry
  labels:
    app: registry
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "local-path"
  resources:
    requests:
      storage: 10Gi
